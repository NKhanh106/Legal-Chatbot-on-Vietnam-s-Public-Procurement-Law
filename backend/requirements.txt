# --- PYTORCH GPU (CUDA 12.4) ---
--extra-index-url https://download.pytorch.org/whl/cu124
torch>=2.4.0  # Install CUDA build on GPU machines
torchvision>=0.19.0
torchaudio>=2.4.0

# Core dependencies for RAG
sentence-transformers>=2.2.2  # For semantic search embeddings
faiss-cpu>=1.7.4  # Windows-friendly; on Linux CUDA, you can swap to faiss-gpu
huggingface_hub[hf_xet]>=0.24.0  # Hugging Face hub with xet support

# Flask API Server
flask>=3.0.0
flask-cors>=4.0.0

# PDF processing (pytesseract + pdf2image)
pytesseract>=0.3.10
pdf2image>=1.16.3
Pillow>=10.0.0

# Advanced Image Preprocessing (for read_pdf.py)
scipy>=1.10.0  # For noise reduction and advanced filters
scikit-learn>=1.2.0  # For deskewing (PCA)

# Word document processing
python-docx>=1.1.0  # For reading .docx files (REQUIRED)
docx2txt>=0.8  # For reading .doc files (REQUIRED for .doc support)

# Vietnamese tokenizer (optional, cho BM25 tốt hơn)
# Chọn một trong hai:
pyvi>=0.1.1  # Hoặc
# underthesea>=1.3.0  # Hoặc cả hai

# BM25 optimization
rank-bm25>=0.2.2  # Fast BM25 implementation with inverted index

# Data processing
numpy>=1.24.0
pandas>=2.0.0
tqdm>=4.65.0  # Progress bars

# Environment variables
python-dotenv>=1.0.0  # Load .env.local files

# Jupyter notebook
jupyter>=1.0.0
ipykernel>=6.25.0

# Groq LLM API
groq>=0.5.0

# Evaluation & Visualization
matplotlib>=3.7.0
seaborn>=0.12.0
openpyxl>=3.1.0 # For reading Excel files

